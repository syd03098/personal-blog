export interface Theme {
  background: {
    main: string;
  };
  text: {
    header: string;
    subHeader: string;
    plain: string;
    smoke: string;
  };
  border: {
    primary100: string;
  };
  palette: {
    emphasis: string;
    selection: string;
    codeBlock: string;
    inlineCodeBlock: string;
  };
}
